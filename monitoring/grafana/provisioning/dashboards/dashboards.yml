apiVersion: 1

providers:
  - name: 'Stock LSTM API Provider'
    orgId: 1
    folder: 'Stock LSTM API'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
